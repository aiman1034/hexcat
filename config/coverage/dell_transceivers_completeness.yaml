brand: Dell
category: transceivers
# WORKFLOW-B CORRECTION (operator L8, 2026-06-15): the andovercg mirror was Dell's © 2017 v1.2 sheet
# (1G-100G only) — a ~3x UNDERCOUNT (the operator's "mirrors go stale" warning, the Juniper/Extreme
# single-source lesson). The CURRENT sheet (© 2026, 19 pp) was fetched via WebFetch past the
# delltechnologies.com 403 -> datasheets/cache/dell-networking-optics-CURRENT.pdf. It spans 1G -> 800G.
# (The 2017 mirror is kept as dell-networking-optics-datasheet.pdf for the undercount audit trail.)
#
# Source structure: optic spec tables pp3-9 (Model | Connector | Wavelength(s) | Media | Distance —
# wavelengths EXPLICIT, no derivation); cable spec tables pp10-12; flat Model|Description denominator
# pp18-19. Parser: _scratch/dell_facts.py (v2, current sheet).
#
complete: false                  # HARVEST CHECKPOINT (current source) — vocab + scope + author pending
captured_count: 182              # 57 optics (1G/10G/25G/40G/100G/200G/400G/800G) + 125 cable length-SKUs
breakdown:
  optics_by_speed: {1G: 4, 10G: 7, 25G: 3, 40G: 4, 100G: 18, 200G: 1, 400G: 11, 800G: 9}
  optics_by_ff: {SFP: 4, SFP+: 7, SFP28: 3, QSFP+: 4, QSFP28: 15, QSFP56: 1, QSFP-DD: 11, OSFP: 9}
scope_flags:
  - {family: SFP-16GFC-SW / Q28-128GFC-SW4, reason_code: out-of-scope, note: "Fibre Channel — catalog-wide FC-OUT policy (operator, SONET-class). Excluded."}
  - {pn: QSA-Q28-S28, reason_code: out-of-scope, note: "QSFP28->SFP28 adapter, not a transceiver. Excluded."}
# OPEN before authoring (RESUME — next focused pass; explicit, flag-don't-fabricate):
#  1. FORM-FACTOR VOCAB GAPS (taxonomy gaps to FIX, lock-step rules.yaml+taxonomy+constants.py + count test):
#     - SFP-DD : S56DD-100G-FR/LR/SR1.2 are 100G SFP-DD MODULES (3 parts; currently mis-mapped to SFP/flagged).
#     - QSFP28-DD : Q28DD-200G/80G-2SR4 (1 part; flagged out pending the token).
#  2. CABLE SCOPE: 125 cable SKUs include generic MPO/LC PASSIVE PATCH cords (CBL-MTP12-OM4-xM, CBL-LC-OM4-xM)
#     + DAC/AOC. MISSION includes DAC/AOC/MPO cables; generic LC-LC fibre jumpers are borderline structured-
#     cabling -> confirm scope (include as MPO/LC Kabel, or flag the LC-LC jumpers). Verify the 125 count.
#  3. MINOR facts cleanup: type-parse gaps (SR1.2/LR1 -> empty type); space-wrapped dual-rate PNs
#     (Q28-100G/40G-PSM4-IR); Gen3/Gen4/FEC variant suffixes kept as distinct SKUs (verify intended).
#  4. THEN: dell_author.py (adapt extreme_author.py — facts-direct optics, DOM media-grounded, Faseranzahl
#     for OSFP/QSFP-DD parallel + duplex, ZR+ tunable=coherent so B.6 passes, Dell PowerSwitch theme,
#     add Dell to rules.yaml vendors) -> backfill -> gate L1-L7 -> ZIP + gate_completeness.yaml -> operator L8.
verdict: Dell re-grounded on the CURRENT 2026 datasheet (corrects a ~3x undercount from the stale 2017
  mirror). 182-SKU full-universe (1G-800G) facts banked with EXPLICIT-grounded wavelengths/connectors;
  2 vocab tokens + cable scope + minor cleanup before author -> gate -> ZIP. Authoring deferred to a
  focused pass (MISSION §9: scope tripled mid-task via Workflow-B; bank verified facts, don't half-build
  a 7-tier brand on depleted context).
