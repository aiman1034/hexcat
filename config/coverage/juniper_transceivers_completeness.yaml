brand: Juniper
category: transceivers
# Source ladder (MISSION.md §7.1) run 2026-06-14 — corrects the stale "JS-gated/blocked" label:
#  a) httpx static: juniper.net doc transceiver pages = prose (no PN table); qualified-optics/*.html
#     = JS-rendered (0 static PNs).
#  b) PDF: datasheets/cache/juniper-optic-modules.pdf — the LEGACY "Optic Modules Matrix"
#     (CTP/E/M/T/MX-era SFP/SFP+/XFP/XENPAK). extract_tables()=0 rows (text-positioned) -> parsed
#     the TEXT via _scratch/juniper_facts.py -> output/stage3/juniper_transceivers_facts.json.
#  c) browser (Chromium, scoped-approved): the qualified-optics JS pages TIME OUT headless even on
#     domcontentloaded (goto never completes — consent/redirect/heavy sync load). -> blocked via
#     these pages.
complete: false
# --- what IS grounded (this PDF) ---
captured_count: 38          # Ethernet SFP/SFP+/XFP/XENPAK optics, grounded + in facts JSON
breakdown:
  by_form_factor: {SFP: 22, SFP+: 4, XFP: 8, XENPAK: 4}
  by_speed: {1G: 18, 100M: 4, 10G: 16}
  eol_candidates: 23        # XENPAK/XFP/E-Series-era — KEPT + flagged informational (§7.2), never dropped
excluded_count: 10          # SONET-only optics (OC-3/OC-12/OC-48) — SKIPPED (operator decision 2026-06-14)
excluded_note: >
  SFP-1OC48-{IR,LR,SR}, SFP-OC12-{IR,LR,SR}, SFP-OC3-{IR,LR,SR} + 1 wrapped row are SONET/SDH optics,
  not Ethernet. Operator ruled: SKIP SONET — out of scope. Permanently excluded (not a gap).
# --- the modern set: SOURCES NOW CACHED (gap RESOLVED to a parse task) ---
modern_optics_sources_cached:   # the JS qualified-optics/HCT pages time out headless; these CURRENT
                                # official guide PDFs are the $0 source instead (ladder rung-b win):
  - datasheets/cache/juniper-100g-optics-guide.pdf   # 100G guide (pub 2026-03-08), 59pp, ~16 PNs (QSFP-100G-DR/FR/ERBD, CFP2-100GBASE-*, JNP-QSFP-100G-*, QSFP-100G-DAC-*)
  - datasheets/cache/juniper-400g-optics-guide.pdf   # 400G guide (pub 2025-07-30), 56pp, ~13 PNs (QDD-400G-DR4/ER4/ZR/AOC, JCO400-QDD-ZR coherent)
  - datasheets/cache/juniper-800g-optics-guide.pdf   # 800G guide, 69pp, ~17 PNs (QDD-800G-*, QDD-2X400G-*)
  # (regex-floor counts; proper per-guide parse will yield the full set. No separate 10G/25G/40G guide
  #  at the URL pattern — 10G/1G covered by the legacy optic-modules.pdf; 25G/40G to confirm during parse.)
verdict: >
  Juniper UNBLOCKED — 38 legacy Ethernet optics already grounded (output/stage3/
  juniper_transceivers_facts.json) AND the modern 100G/400G/800G guides are now cached (above),
  correcting the earlier "JS-blocked" diagnosis. RESUME: parse the 3 modern guides (text-mode, like
  the legacy PDF — extract PN/speed/type/reach/wavelength/connector) → merge with the 38 legacy →
  author Juniper as ONE batch → gate → ZIP → operator audit. complete:false until the parse + author land.
