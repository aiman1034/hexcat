# Cisco transceiver datasheet frontier — Claude appends NEW datasheet URLs here from
# in-session WebSearch ("Cisco <family> transceiver datasheet"). One URL per line.
# lib/harvest.py folds these in as additional discovery seeds on the next run.
# Lines starting with '#' and blank lines are ignored. $0: no paid API produces this.
